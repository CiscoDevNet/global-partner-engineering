# This script takes the api credentials and the FMC IP which needs to be polled and produces an excel sheet as an output containing details of the TID incidents.
# python script.py <api_username> <api_password> <FMC_IP>
# As an input it will ask you the 'From' and 'To' period for pulling the report. Specify in local timezone in 24hr format.
